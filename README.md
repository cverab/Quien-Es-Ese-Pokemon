# ¿Quién es ese Pokémon? 🎮

Un juego interactivo en React inspirado en el clásico reto de adivinar Pokémon.  
El jugador debe identificar correctamente al Pokémon mostrado en pantalla antes de que se acabe el tiempo.  
El proyecto utiliza la **PokeAPI** para obtener datos en tiempo real.

---

## 🚀 Características
- Juego de preguntas tipo quiz: "¿Quién es ese Pokémon?"
- Imágenes oficiales de Pokémon obtenidas desde la PokeAPI.
- Opciones de respuesta aleatorias (1 correcta + 3 falsas).# ¿Quién es ese Pokémon? 🎮

Un juego interactivo en React inspirado en el clásico reto de adivinar Pokémon.  
El jugador debe identificar correctamente al Pokémon mostrado en pantalla antes de que se acabe el tiempo.  
El proyecto utiliza la **PokeAPI** para obtener datos en tiempo real.

---

## 🚀 Características
- Juego de preguntas tipo quiz: "¿Quién es ese Pokémon?"
- Imágenes oficiales de Pokémon obtenidas desde la PokeAPI.
- Opciones de respuesta aleatorias (1 correcta + 3 falsas).
- Temporizador de 60 segundos por partida.
- Sistema de puntaje acumulativo.
- Tabla de clasificación persistente (guardada en `localStorage`).
- Layout con tres columnas:  
  - Imagen izquierda  
  - Contenido principal  
  - Imagen derecha  
- **Novedades recientes:**
  - Se agregó la **novena generación** de Pokémon.
  - Se mejoró la visibilidad del texto del **contador de tiempo restante**.
  - Se optimizó el **tiempo de carga** entre Pokémon para una experiencia más fluida.

---

## 🛠️ Tecnologías utilizadas
- **React** (Hooks: `useState`, `useEffect`)
- **Axios** para llamadas HTTP
- **CSS Flexbox/Grid** para el diseño responsivo
- **LocalStorage** para persistencia de datos

---

## 📂 Estructura del proyecto
src/
├── App.js        # Componente principal del juego  
├── App.css       # Estilos principales  
├── assets/       # Imágenes estáticas (logo, banners, etc.)  
└── index.js      # Punto de entrada de React  

- Temporizador de 60 segundos por partida.
- Sistema de puntaje acumulativo.
- Tabla de clasificación persistente (guardada en `localStorage`).
- Layout con tres columnas:  
  - Imagen izquierda  
  - Contenido principal  
  - Imagen derecha  

---

## 🛠️ Tecnologías utilizadas
- **React** (Hooks: `useState`, `useEffect`)
- **Axios** para llamadas HTTP
- **CSS Flexbox/Grid** para el diseño responsivo
- **LocalStorage** para persistencia de datos

---

## 📂 Estructura del proyecto
src/
├── App.js        # Componente principal del juego
├── App.css       # Estilos principales
├── assets/       # Imágenes estáticas (logo, banners, etc.)
└── index.js      # Punto de entrada de React
